# Replace this file with the hardware-configuration.nix generated on the target machine.
#
# Run on the new machine:
#   sudo nixos-generate-config --show-hardware-config > /tmp/hardware-configuration.nix
# Then copy the generated file here.

{ ... }:

{
  # Placeholder only. Do not build this template before replacing this file.
}
